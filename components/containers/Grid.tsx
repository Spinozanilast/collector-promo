const Grid = (sectionName: string) => {
    return (
        <section id="topics-grid">
            <h1 id="section-name" className="centered-text">
                {sectionName}
            </h1>
            <div></div>
        </section>
    );
};

export default Grid;
